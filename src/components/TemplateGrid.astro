---
// TemplateGrid.astro
// ─────────────────────────────────────────────────────────────
// Renders the searchable/filterable product grid section plus the
// shared product detail modal.
//
// This component is intentionally "dumb": it only composes markup.
//   • Product data              → src/data/templates.js
//   • Per-card markup           → TemplateCard.astro
//   • Modal markup              → ProductModal.astro
//   • Search/filter/modal logic → src/scripts/templateGrid.js
//   • Modal-specific CSS        → src/styles/template-grid.css
// ─────────────────────────────────────────────────────────────

import TemplateCard from './TemplateCard.astro';
import ProductModal from './ProductModal.astro';
import templates, { MESSENGER_URL } from '../data/templates.js';
import '../styles/template-grid.css';

// Categories rendered as filter pills above the grid. "All" is always
// selected by default (index 0), matching the original behaviour.
const FILTER_CATEGORIES = [
  'All',
  'Business',
  'Marketing',
  'Resume',
  'Social Media',
  'Website',
  'Presentation',
  'Productivity',
];

/**
 * Computes the 1-4 stagger index used by the `.reveal-delay-N` classes,
 * matching the original inline `Math.min(i % 4 + 1, 4)` expression.
 */
function getRevealDelayIndex(cardIndex: number) {
  return Math.min((cardIndex % 4) + 1, 4);
}
---

<!-- ═══════════════════════════════════════════════
     SECTION — template grid
════════════════════════════════════════════════ -->
<section id="templates" class="pt-10 pb-20 lg:pt-14 lg:pb-28 relative overflow-hidden" style="background:var(--bg);">
  <div
    class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[700px] h-[400px] rounded-full blur-3xl pointer-events-none"
    style="background:rgba(0,95,115,0.04);"
    aria-hidden="true"
  ></div>

  <div class="container-xl relative">

    <!-- Section header -->
    <div class="text-center mb-10 reveal">
      <span class="eyebrow mb-3">
        <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 12 12" aria-hidden="true">
          <rect width="5" height="5" rx="1"/>
          <rect x="7" width="5" height="5" rx="1"/>
          <rect y="7" width="5" height="5" rx="1"/>
          <rect x="7" y="7" width="5" height="5" rx="1"/>
        </svg>
        Our Templates
      </span>
      <h2 class="section-heading text-3xl sm:text-4xl lg:text-5xl mt-2 mb-3">
        Ready-Made Templates<br class="hidden sm:block" />
        <span class="text-teal-gradient">Built for Professionals</span>
      </h2>
      <p class="text-lg max-w-2xl mx-auto" style="color:var(--text-secondary);">
        Ready to Stand Out in the Competitive Job Market?
      </p>
    </div>

    <!-- Search bar -->
    <div class="relative max-w-xl mx-auto mb-8 reveal">
      <svg
        class="absolute left-4 top-1/2 -translate-y-1/2 w-4 h-4 pointer-events-none"
        fill="none" viewBox="0 0 24 24" stroke-width="2"
        style="color:var(--teal-primary);"
        aria-hidden="true"
      >
        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" d="M21 21l-4.35-4.35m1.1-5.4a6.5 6.5 0 11-13 0 6.5 6.5 0 0113 0z"/>
      </svg>
      <input
        id="template-search"
        type="search"
        placeholder="Search templates…"
        class="input w-full pl-11 pr-4"
        style="border-radius:9999px; padding-top:0.75rem; padding-bottom:0.75rem; box-shadow:0 4px 20px rgba(0,95,115,0.08);"
        aria-label="Search templates"
      />
    </div>

    <!-- Category filters -->
    <div
      class="flex flex-wrap items-center justify-center gap-2 mb-10 reveal reveal-delay-1"
      role="group"
      aria-label="Filter templates by category"
    >
      {FILTER_CATEGORIES.map((cat, i) => (
        <button
          class="filter-btn font-display font-medium rounded-full px-4 py-1.5 text-sm transition-all duration-200 border"
          style={i === 0
            ? 'background:var(--teal-primary); color:#fff; border-color:var(--teal-primary);'
            : 'background:transparent; color:var(--text-secondary); border-color:var(--border-mid);'}
          data-filter={cat}
          aria-pressed={i === 0 ? 'true' : 'false'}
        >
          {cat}
        </button>
      ))}
    </div>

    <!-- No-results message -->
    <div
      id="no-results"
      class="hidden text-center py-16"
      role="status"
      aria-live="polite"
    >
      <p class="text-4xl mb-3" aria-hidden="true">🔍</p>
      <p class="font-display font-semibold" style="color:var(--charcoal);">No templates found</p>
      <p class="text-sm mt-1" style="color:var(--text-muted);">Try a different search term or browse all categories.</p>
    </div>

    <!-- Template grid -->
    <div
      id="template-grid"
      class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 sm:gap-5"
    >
      {templates.map((template, i) => (
        <TemplateCard template={template} revealDelayIndex={getRevealDelayIndex(i)} />
      ))}
    </div>

    <!-- Bottom CTA -->
    <div class="text-center mt-12 reveal">
      <p class="mb-4" style="color:var(--text-secondary);">
        Don't see what you need? We create custom templates too.
      </p>
      <a
        href={MESSENGER_URL}
        target="_blank"
        rel="noopener noreferrer"
        class="btn btn-secondary btn-lg inline-flex"
      >
        Request a Custom Template
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
        </svg>
      </a>
    </div>

  </div>
</section>

<ProductModal messengerUrl={MESSENGER_URL} />

<script src="../scripts/templateGrid.js"></script>
